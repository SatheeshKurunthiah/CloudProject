1. List Data Script (list_data.sh)

This script will take all the object stored in S3 bucket and print as a Key Value Pair.

2. Run Process Messages as a Service (run_java_server.sh)

This script will run the Process Messages java application as service to handle system booting.

3. Run Instance Manager as a Service (run_manager.sh)

This script will run the Process Messages java application as service to handle system booting.

4. Run Store Messages as a Service (run_s3_server.sh)

This script will run the Process Messages java application as service to handle system booting.

5.  Run Tensor flow application (run_tensor.sh)

This script will activate the tensor flow application, runs for the given output and returns the output.
